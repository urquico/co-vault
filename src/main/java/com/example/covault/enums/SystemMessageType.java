package com.example.covault.enums;

public enum SystemMessageType {
    SUCCESS,
    CREATED,
    UNAUTHORIZED,
    USER_NOT_FOUND,
    SERVER_ERROR,
    TOKEN_NOT_FOUND,
    TOKEN_NOT_FOUND_OR_EXPIRED,
    INCORRECT_PASSWORD,
}
